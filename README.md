# threaded-port-scanner
A Python-based multi-threaded port scanner that checks the first 1024 ports faster than your patience runs out.  Uses threads because waiting one port at a time is for beginners.  Loud, fast, and straight to the point.
